<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <CheckBoxPreference
        android:key="power_save_mode"
        android:title="@string/pref_power_save"
        android:summary="@string/pref_description_power_save"
        android:defaultValue="false" />

    <ListPreference
        android:key="screen_mode"
        android:title="@string/pref_keep_screen_on"
        android:entries="@array/pref_screen_mode_entries"
        android:entryValues="@array/pref_screen_mode_values"
        android:defaultValue="1"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
